@model IEnumerable<FlightBooking1.Models.Plane>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeOfPlane)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeOfPlane)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlaneID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlaneID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlaneID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
